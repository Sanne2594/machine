#!/usr/bin/env bash

#Train een model op de data parallel aan de masks.

#Evaluate het model voor een sanity check dat het goed werkt.

#Somehow run the model on the inputs word by word and save the [hidden layer] + mask of that word.